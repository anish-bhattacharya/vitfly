# Mamba 变体适配报告 - 无人机避障任务

> **生成日期**: 2026-03-23  
> **目标任务**: 四旋翼无人机端到端视觉避障 (Vision-Based Quadrotor Obstacle Avoidance)  
> **当前基线**: ViT-LSTM (3.6M 参数), DroneMamba (452K 参数)

---

## 📊 调研变体概览

| 变体 | 核心特性 | 参数量 | 视觉任务 | 任意分辨率 | RL/BC 支持 | 适配度 |
|------|---------|--------|---------|-----------|------------|--------|
| **DecisionMamba** | 多粒度 SSM + 自进化策略 | ~50M | ❌ | ❌ | ✅ 离线 RL | ⭐⭐⭐⭐ |
| **MAMBA (MS)** | 多专家模仿学习 | 视任务 | ❌ | ❌ | ✅ 模仿学习 | ⭐⭐⭐ |
| **MambaVision** | 混合 Mamba-Transformer | 31-740M | ✅ | ✅ | ❌ | ⭐⭐⭐⭐⭐ |
| **mamba3-minimal** | Mamba-3 最新架构 | 可配置 | ⚠️ 需适配 | ⚠️ 需适配 | ⚠️ 基础 | ⭐⭐⭐⭐ |
| **VMamba** | 2D 选择性扫描 | 28-89M | ✅ | ⚠️ 有限 | ❌ | ⭐⭐⭐⭐⭐ |

---

## 🔬 各变体详细分析

### 1. DecisionMamba ⭐⭐⭐⭐

**论文**: NeurIPS 2024

#### 架构特点
```
输入状态序列
    │
    ▼
┌─────────────────────────────────────┐
│  粗粒度 SSM (Mamba 架构)            │
│  - 建模历史隐藏状态                  │
│  - 提取时间依赖信息                  │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  细粒度 SSM 模块                    │
│  - 状态-动作-RTG 三元组关系建模       │
│  - 捕捉奖励信号与动作的关联           │
└─────────────────────────────────────┘
    │
    ▼
自进化策略 (Self-Evolving Policy)
    │
    ▼
输出: 动作策略
```

#### 关键创新
| 创新 | 作用 |
|------|------|
| 多粒度 SSM | 同时建模时序和状态-动作关系 |
| 自进化策略 | 缓解噪声演示过拟合 |
| 渐进正则化 | 增强鲁棒性 |

#### 适配建议
- **可直接借鉴**: 多粒度 SSM 设计思想
- **可迁移**: 自进化策略 → 提升对困难场景的鲁棒性
- **需修改**: 输入为状态序列，非图像

#### 适配难度: ⭐⭐⭐ (中等)

---

### 2. MAMBA (Microsoft) ⭐⭐⭐

#### 架构特点
- **多专家模仿学习框架**: 从多个专家策略学习
- **专家等级可调**: 支持不同质量专家的组合
- **基于 TensorFlow 2 + DartEnv**

#### 关键创新
| 创新 | 作用 |
|------|------|
| 多专家融合 | 整合不同水平专家知识 |
| 策略改进 | 从次优专家学习改进策略 |

#### 适配建议
- **可直接借鉴**: 多专家训练框架
- **不适用**: 架构设计偏重 RL 环境，非视觉任务

#### 适配难度: ⭐⭐ (较低)

---

### 3. MambaVision ⭐⭐⭐⭐⭐

**论文**: arXiv:2407.08083 | **SOTA 视觉骨干**

#### 架构特点
```
输入图像 (任意分辨率)
    │
    ▼
┌─────────────────────────────────────┐
│  Stage 1: Patch Embedding           │
│  (Conv 3→64, 4×4, stride 4)         │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  Stage 2-5: 4 阶段分层特征提取       │
│  ┌─────────────────────────────┐    │
│  │ VSS Block (Mamba)           │    │
│  │ + Self-Attention (对称路径)  │    │
│  └─────────────────────────────┘    │
│  逐步降采样: 4→2→1                   │
└─────────────────────────────────────┘
    │
    ▼
输出: 多尺度分层特征
```

#### 性能指标
| 模型 | 参数量 | FLOPs | Top-1 Acc | 吞吐量 |
|------|--------|-------|-----------|--------|
| MambaVision-T | 31.8M | 4.4G | 82.3% | 6298 img/s |
| MambaVision-S | 50.1M | 7.5G | 83.3% | 4700 img/s |
| MambaVision-B | 97.7M | 15.0G | 84.2% | 3670 img/s |

#### 关键创新
| 创新 | 作用 |
|------|------|
| **任意分辨率输入** | 无需修改模型即可处理任意尺寸图像 |
| **混合 Mamba-Transformer** | 结合 SSM 全局建模 + Transformer 细节捕捉 |
| **对称路径 Mixer** | 增强全局上下文建模能力 |
| **分层特征输出** | 适配检测/分割等下游任务 |

#### 适配建议
- **⭐ 强烈推荐**: 任意分辨率特性完美适配无人机避障
- **可直接使用**: 作为视觉编码器
- **建议变体**: MambaVision-T (小) 或 MambaVision-S (中)
- **输出适配**: 移除分类头，添加时序建模层 (LSTM/SSM)

#### 适配难度: ⭐⭐ (很低)

---

### 4. mamba3-minimal ⭐⭐⭐⭐

**特点**: 纯 PyTorch 实现 Mamba-3 最新架构 (~800 行)

#### 架构特点
```
输入 Token
    │
    ▼
Embedding Layer
    │
    ▼
┌─────────────────────────────────────┐
│  × n_layer (交替块)                 │
│  ┌─────────────────────────────┐    │
│  │ RMSNorm → Mamba3 SSM        │    │
│  │ - 梯形离散化 (二阶精度)       │    │
│  │ - 复数 SSM + RoPE           │    │
│  │ - B,C QK-Normalization      │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ RMSNorm → SwiGLU MLP        │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
    │
    ▼
输出: LM Head (权重绑定)
```

#### Mamba-3 关键创新
| 创新 | 作用 |
|------|------|
| **梯形离散化** | 二阶精度状态更新 (vs 一阶 Euler) |
| **复数 SSM + RoPE** | 更好的状态追踪能力 |
| **B,C QK-Normalization** | 提升训练稳定性 |
| **MIMO (Rank-R)** | 秩-R 状态更新，提高表达能力 |
| **无短卷积** | 简化架构 |

#### 适配建议
- **可直接借鉴**: Mamba-3 SSM 核心实现
- **需适配**: 纯语言模型，需添加视觉编码器
- **推荐组合**: CNN 编码器 + Mamba-3 SSM 时序建模

#### 适配难度: ⭐⭐⭐⭐ (较高)

---

### 5. VMamba ⭐⭐⭐⭐⭐

**论文**: NeurIPS 2024 Spotlight

#### 架构特点
```
输入图像
    │
    ▼
┌─────────────────────────────────────┐
│  Patch Embedding                    │
│  (Conv 3→96, stride 4)              │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  VSS Block × N (堆叠)               │
│  ┌─────────────────────────────┐    │
│  │ 2D 选择性扫描 (SS2D)        │    │
│  │ - 4 条扫描路径              │    │
│  │ - 捕捉多方向上下文           │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ MLP Block                   │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
    │
    ▼
输出: 分层特征
```

#### 性能指标
| 模型 | 分辨率 | Top-1 | 参数量 | COCO 检测 mAP |
|------|--------|-------|--------|---------------|
| VMamba-S | 224 | 83.6% | 50M | 48.7 |
| VMamba-B | 224 | 83.9% | 89M | 49.2 |

#### 关键创新
| 创新 | 作用 |
|------|------|
| **2D 选择性扫描 (SS2D)** | 解决 1D SSM 与 2D 图像的结构差异 |
| **四向扫描** | 收集多方向、多视角上下文信息 |
| **线性时间复杂度** | O(N) vs Transformer 的 O(N²) |
| **全局有效感受野** | 适合捕捉远距离障碍物关系 |

#### 适配建议
- **⭐ 强烈推荐**: 专为视觉任务设计
- **可直接使用**: 作为视觉编码器
- **推荐变体**: VMamba-T (轻量) 或 VMamba-S (平衡)
- **输出适配**: 添加时序建模层 (LSTM/SSM)

#### 适配难度: ⭐⭐ (很低)

---

## 🎯 适配建议总结

### 推荐优先级排序

| 优先级 | 变体 | 理由 | 预计工作量 |
|--------|------|------|-----------|
| 🥇 1 | **VMamba** | 专为视觉设计，性能优秀，适配简单 | 1-2 天 |
| 🥇 2 | **MambaVision** | 任意分辨率 + 混合架构，完美适配 | 1-2 天 |
| 🥇 3 | **mamba3-minimal** | 最新架构，可定制性强 | 3-5 天 |
| 🥈 4 | **DecisionMamba** | RL/BC 思想可借鉴 | 5-7 天 |
| 🥉 5 | **MAMBA (MS)** | 多专家框架，架构不直接适用 | 3-5 天 |

### 推荐的混合架构设计

#### 方案 A: VMamba + LSTM (推荐)
```
深度图像 (60×90)
    │
    ▼
┌─────────────────────────────────────┐
│  VMamba-T 视觉编码器                │
│  (输出分层特征)                     │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  特征投影层                         │
│  (768 → 512)                       │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  融合层                            │
│  concat [特征, des_vel/10, quat]   │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  LSTM 时序建模                      │
│  (input=519, hidden=128)           │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  输出层                            │
│  FC(128 → 3) 速度命令               │
└─────────────────────────────────────┘
```

**预估参数量**: ~50M (VMamba) + 0.6M (LSTM) ≈ **50.6M**

#### 方案 B: MambaVision-T + SSM
```
深度图像 (任意分辨率)
    │
    ▼
┌─────────────────────────────────────┐
│  MambaVision-T 编码器               │
│  (31.8M 参数)                       │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  Mamba-3 SSM 时序建模               │
│  (替代 LSTM)                        │
└─────────────────────────────────────┘
    │
    ▼
输出: 速度命令
```

**预估参数量**: ~32M (MambaVision) + ~0.5M (SSM) ≈ **32.5M**

#### 方案 C: CNN + Mamba-3 SSM (轻量)
```
深度图像 (60×90)
    │
    ▼
┌─────────────────────────────────────┐
│  CNN 特征提取 (当前 DroneMamba)     │
│  (Conv 1→32→64, 池化)              │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  Mamba-3 SSM 时序建模               │
│  - 梯形离散化                       │
│  - 复数 SSM + RoPE                 │
└─────────────────────────────────────┘
    │
    ▼
输出: 速度命令
```

**预估参数量**: ~0.4M (CNN) + ~0.5M (Mamba-3) ≈ **0.9M**

---

## 🔬 消融实验建议

### 实验设计

| 实验组 | 视觉编码器 | 时序建模 | 预期目标 |
|--------|-----------|---------|---------|
| Baseline | ViT-LSTM | LSTM | 当前 SOTA |
| Exp-1 | VMamba-T | LSTM | 验证 VMamba 编码能力 |
| Exp-2 | VMamba-T | SSM | 验证 SSM vs LSTM |
| Exp-3 | MambaVision-T | SSM | 验证混合架构 |
| Exp-4 | CNN | Mamba-3 SSM | 轻量方案对比 |
| Exp-5 | CNN | LSTM | 当前 DroneMamba 改进版 |

### 评估指标

1. **成功率**: 到达目标且无碰撞的比例
2. **碰撞率**: 碰撞次数 / 总测试次数
3. **完成时间**: 平均到达目标的时间
4. **推理速度**: ms/帧 (实时性要求 <20ms)
5. **参数量**: 模型大小 (边缘部署考虑)

---

## 📋 下一步行动

1. **立即可行**: 使用 VMamba-S 替换当前 CNN 编码器
2. **短期**: 实现 MambaVision-T + SSM 混合架构
3. **中期**: 尝试 mamba3-minimal 的 SSM 实现
4. **长期**: 借鉴 DecisionMamba 的多粒度思想

---

**报告完成** ✅

如需开始实现某个方案，请告知！
